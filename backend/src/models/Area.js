class Area {
  constructor({ id, nombre, activo, createdAt, updatedAt }) {
    this.id = id;
    this.nombre = nombre;
    this.activo = activo;
    this.createdAt = createdAt;
    this.updatedAt = updatedAt;
  }
}

module.exports = Area;
